// src/components/Manufacturer/Orders/OrderDetail.js

import React from 'react';
import { Box, Typography } from '@mui/material';

const OrderDetail = () => {
 
  return (
    <Box p={2}>
      <Typography variant="h4" gutterBottom>Order Details</Typography>
    </Box>
  );
};

export default OrderDetail;
